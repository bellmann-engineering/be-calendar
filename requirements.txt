# =============================================================================
# requirements.txt – NUR Laufzeit-Abhängigkeiten (landen im Docker-Image)
# -----------------------------------------------------------------------------
# Alle Versionen sind mit "==" fest gepinnt: Jeder Build installiert exakt
# dieselben Pakete (reproduzierbar, keine Überraschungen durch neue Releases).
# Entwicklungs-/Test-Werkzeuge stehen in requirements-dev.txt.
# Update-Vorgehen: Version hier erhöhen -> Tests lokal + in CI -> committen.
# =============================================================================

# --- Web-Framework Core ---
Flask==3.0.2                       # Web-Framework (Routing, Templates, Requests)

# --- Datenbank & ORM ---
Flask-SQLAlchemy==3.1.1            # ORM: PostgreSQL-Tabellen <-> Python-Klassen
psycopg[binary]==3.3.6             # PostgreSQL-Treiber v3 (binary = ohne Compiler installierbar)
Flask-Migrate==4.0.5               # Alembic-Integration: "flask db upgrade"

# --- Sicherheit & Authentifizierung ---
Flask-JWT-Extended==4.6.0          # JWT in HttpOnly-Cookies inkl. CSRF-Schutz
Flask-Limiter==3.5.0               # Rate-Limiting (Login, Passwort-Reset, CSV)

# --- Umgebungsvariablen & Konfiguration ---
python-dotenv==1.0.1               # Lädt .env in os.environ
tzdata==2026.4                     # Zeitzonendatenbank für zoneinfo (fehlt im slim-Image)

# --- Produktions-Server ---
gunicorn==26.2.0                   # WSGI-Server (gthread-Worker, siehe gunicorn.conf.py)

# --- Google Calendar API ---
cryptography==50.0.1               # verschlüsselt das Google-Refresh-Token in der Datenbank
requests==2.34.2                   # Token-Widerruf und Kontoadresse bei Google (OAuth)
google-api-python-client==2.118.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0

# --- Bilder ---
Pillow==12.3.0                     # Kunden-Logos: prüfen, verkleinern, neu als PNG kodieren
