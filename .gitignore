# Python & Umgebungen
venv/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Umgebungsvariablen und Geheimnisse (Vorlage .env.example IST versioniert)
.env
.env.*
!.env.example
secrets/
nginx/certs/
google_credentials.json

# Lokale Compose-Anpassungen (z. B. DB-Port für DBeaver)
docker-compose.override.yml

# Datenbanken und Docker-Volumes
postgres_data/

# IDEs und Betriebssysteme
.vscode/
.idea/
.DS_Store

# Persönliche Lern- und Diagnosewerkzeuge (nicht Teil des Firmenprojekts)
devtools/

# Frontend: Abhängigkeiten und Build-Ergebnis (npm ci && npm run build erzeugt beides neu)
node_modules/
app/static/dist/

# Temporär generierte Dateien
projekt_status*.txt
